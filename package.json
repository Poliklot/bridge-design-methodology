{
  "name": "bridge-design-methodology",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "content:sync": "node scripts/sync-site-content.mjs",
    "lint:ru": "node scripts/check-russian-copy.mjs",
    "predev": "npm run content:sync",
    "dev": "astro dev",
    "prebuild": "npm run content:sync && npm run lint:ru",
    "build": "astro build",
    "check": "npm run content:sync && astro check",
    "preview": "astro preview",
    "test": "npm run check && npm run build"
  },
  "dependencies": {
    "@astrojs/starlight": "0.41.3",
    "@fontsource-variable/golos-text": "5.2.8",
    "@fontsource/ibm-plex-mono": "5.2.7",
    "astro": "7.0.9",
    "sharp": "0.35.3"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.9",
    "typescript": "6.0.3"
  }
}
