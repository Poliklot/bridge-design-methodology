{
  "methodology": "BRIDGE",
  "version": "0.1.4",
  "severityLevels": [
    "error",
    "warning",
    "info"
  ],
  "automationLevels": [
    "automatic",
    "heuristic",
    "manual"
  ],
  "rules": [
    {
      "id": "identity.missing-stable-identity",
      "principle": "Identity",
      "severity": "error",
      "automation": "automatic",
      "description": "Important transferable elements must have a stable identity: a layer name or a required BRIDGE tag.",
      "fix": "Use an English kebab-case layer name when Figma metadata provides the type; use a BRIDGE tag only for intent Figma does not know."
    },
    {
      "id": "identity.duplicate-identity",
      "principle": "Identity",
      "severity": "error",
      "automation": "automatic",
      "description": "Two elements inside the same breakpoint/view scope must not share one stable identity.",
      "fix": "Rename identities or add instance indexes such as `product-card-1`, `product-card-2`."
    },
    {
      "id": "identity.same-identity-different-type",
      "principle": "Identity",
      "severity": "error",
      "automation": "automatic",
      "description": "The same identity must not resolve to different logical element types across breakpoints.",
      "fix": "Keep the Figma node/component type stable or model a separate entity."
    },
    {
      "id": "identity.repeated-item-no-index",
      "principle": "Identity",
      "severity": "error",
      "automation": "heuristic",
      "description": "Repeated list/grid items need stable instance identities.",
      "fix": "Use indexed identities such as `feature-card-1`, `feature-card-2`."
    },
    {
      "id": "section.role-prefix-in-name",
      "principle": "Identity",
      "severity": "warning",
      "automation": "heuristic",
      "description": "Section layer names should not repeat the role with prefixes such as `Section /` or `Секция /`.",
      "fix": "Keep the human label concise and express the section contract with `[section=...]`."
    },
    {
      "id": "section.identity-as-human-heading",
      "principle": "Identity",
      "severity": "warning",
      "automation": "heuristic",
      "description": "`[section=...]` should describe the reusable section/component contract, not copy the page-specific heading.",
      "fix": "Use the layer name for the contextual heading and keep `[section=...]` stable for the reusable section contract, for example `Related products [section=product-slider]`."
    },
    {
      "id": "naming.default-layer-name",
      "principle": "Roles",
      "severity": "warning",
      "automation": "heuristic",
      "description": "Default names such as `Frame 53` or `Group 271` usually hide missing structure or intent.",
      "fix": "Rename the layer with a stable English kebab-case identity; add BRIDGE intent tags only where needed."
    },
    {
      "id": "layout.missing-role",
      "principle": "Roles",
      "severity": "error",
      "automation": "automatic",
      "description": "Every layer must be classifiable from Figma metadata or BRIDGE intent tags.",
      "fix": "Fix the Figma structure/metadata first; add BRIDGE tags only for product intent such as `[section=...]`, `[link=...]`, `[control=...]`, `[field=...]`, `[decor=...]`, `[asset=...]`, `[modal=...]`, or `[state=...]`."
    },
    {
      "id": "layout.positioned-without-intent",
      "principle": "Exceptions",
      "severity": "warning",
      "automation": "automatic",
      "description": "Visually detached or overlapping elements must have clear intent.",
      "fix": "Use Figma positioning for geometry and add intent only when needed: `[decor=...]`, `[asset=...]`, `[modal=...]`, `[state=...]`, or `[bridge-exception=...] [reason=...]`."
    },
    {
      "id": "layout.one-child-wrapper-without-role",
      "principle": "Roles",
      "severity": "warning",
      "automation": "heuristic",
      "description": "A one-child wrapper without a structural reason is likely unnecessary noise.",
      "fix": "Remove the wrapper or make its Figma purpose clear through structure, naming, clipping, shared surface, target scope, or asset grouping."
    },
    {
      "id": "layout.wrapper-chain-too-deep",
      "principle": "Roles",
      "severity": "warning",
      "automation": "heuristic",
      "description": "Deep wrapper chains often make responsive mapping fragile.",
      "fix": "Flatten structure or ensure each wrapper has a real Figma purpose."
    },
    {
      "id": "layout.overlap-without-overlay-role",
      "principle": "Geometry",
      "severity": "warning",
      "automation": "heuristic",
      "description": "Overlapping bounds require clear overlay/decor/asset intent or an explicit exception.",
      "fix": "Declare `[decor=...]`, `[asset=...]`, a target tag, or a BRIDGE exception reason. Positioning comes from Figma metadata."
    },
    {
      "id": "layout.z-index-ambiguous",
      "principle": "Geometry",
      "severity": "warning",
      "automation": "heuristic",
      "description": "Layer order is visually important but not declared as overlay/stacking intent.",
      "fix": "Declare overlay scope or stacking behavior."
    },
    {
      "id": "responsive.missing-breakpoint-root",
      "principle": "Breakpoints",
      "severity": "error",
      "automation": "automatic",
      "description": "Responsive root frames must declare `[bp=...]`.",
      "fix": "Add explicit breakpoint tags to root frames."
    },
    {
      "id": "responsive.identity-missing-in-required-breakpoint",
      "principle": "Breakpoints",
      "severity": "error",
      "automation": "automatic",
      "description": "An important identity exists on one breakpoint but is missing on another required breakpoint.",
      "fix": "Keep the same identity in the same parent and change breakpoint visibility instead of deleting it, or model a separate state/variant/collection rule/structural exception."
    },
    {
      "id": "responsive.tree-cardinality-changed",
      "principle": "Breakpoints",
      "severity": "error",
      "automation": "automatic",
      "description": "Responsive breakpoint roots for the same view do not preserve the same logical element tree/cardinality.",
      "fix": "Keep the same transferable identities across breakpoints, or model the difference as a collection rule, state, variant, or structural exception."
    },
    {
      "id": "responsive.parent-changed-across-breakpoints",
      "principle": "Breakpoints",
      "severity": "error",
      "automation": "automatic",
      "description": "A transferable identity changes parent identity across breakpoints.",
      "fix": "Preserve parent-child topology, use shared wrappers, or declare an explicit structural exception."
    },
    {
      "id": "responsive.order-changed-without-reason",
      "principle": "Breakpoints",
      "severity": "warning",
      "automation": "heuristic",
      "description": "Sibling order changed across breakpoints without an order strategy.",
      "fix": "Preserve semantic order or declare an order strategy."
    },
    {
      "id": "content.text-changed-between-breakpoints",
      "principle": "Identity",
      "severity": "error",
      "automation": "heuristic",
      "description": "The same text identity has different content across breakpoints.",
      "fix": "Keep responsive text identical. Model locale, experiment, or product variants outside breakpoint variants."
    },
    {
      "id": "content.manual-line-break-in-dynamic-text",
      "principle": "Dependencies",
      "severity": "error",
      "automation": "heuristic",
      "description": "Dynamic text must not rely on forced line breaks or <br> to fit the layout.",
      "fix": "Let text wrap by its container using hug/min-height, max-width, and explicit overflow/truncation rules. Use `[bridge-exception=manual-line-break] [reason=...]` only for semantic or approved brand-lockup breaks."
    },
    {
      "id": "content.manual-line-break-exception-without-reason",
      "principle": "Exceptions",
      "severity": "error",
      "automation": "automatic",
      "description": "A manual line break exception must explain why the break is semantic or brand-required.",
      "fix": "Pair `[bridge-exception=manual-line-break]` with `[reason=...]`, for example `[reason=brand-lockup]` or `[reason=postal-address-format]`."
    },
    {
      "id": "content.strict-content-drift",
      "principle": "Dependencies",
      "severity": "error",
      "automation": "heuristic",
      "description": "Strict content such as price, legal copy, or CTA changed across breakpoints.",
      "fix": "Synchronize strict content or document an approved variant."
    },
    {
      "id": "content.hidden-layer-source-of-truth",
      "principle": "Exceptions",
      "severity": "error",
      "automation": "heuristic",
      "description": "Hidden keyed layers may contain alternative source-of-truth content.",
      "fix": "Move alternatives into declared states/variants or archive them."
    },
    {
      "id": "interaction.control-without-action",
      "principle": "Dependencies",
      "severity": "error",
      "automation": "automatic",
      "description": "Controls must declare an action. Links must declare href instead.",
      "fix": "Use `[href=...]` for links. Use `[action=modal:...]`, `[action=state:...]`, `[action=submit:...]`, `[action=reset:...]`, or `[action=none]` for controls."
    },
    {
      "id": "interaction.link-without-href",
      "principle": "Dependencies",
      "severity": "error",
      "automation": "automatic",
      "description": "Link elements must declare href as the only destination truth.",
      "fix": "Add `[href=...]`. Do not add `[to=...]` or `[action=link:...]`."
    },
    {
      "id": "interaction.link-has-second-destination",
      "principle": "Dependencies",
      "severity": "error",
      "automation": "automatic",
      "description": "Links must not declare a second destination such as `[to=...]` or `[action=link:...]` alongside href.",
      "fix": "Keep only `[href=...]` for navigation destination."
    },
    {
      "id": "interaction.modal-target-missing",
      "principle": "Dependencies",
      "severity": "error",
      "automation": "automatic",
      "description": "A modal action must point to an existing modal target.",
      "fix": "Create `[modal=target-id]` target or change the action."
    },
    {
      "id": "interaction.state-target-missing",
      "principle": "Dependencies",
      "severity": "error",
      "automation": "automatic",
      "description": "A state action must point to an existing state target.",
      "fix": "Create `[state=target-id]` target or change the action."
    },
    {
      "id": "interaction.modal-missing-close",
      "principle": "Dependencies",
      "severity": "error",
      "automation": "heuristic",
      "description": "Modal targets need close behavior.",
      "fix": "Add close button/action and define backdrop/escape behavior."
    },
    {
      "id": "interaction.form-field-missing-label",
      "principle": "Dependencies",
      "severity": "warning",
      "automation": "automatic",
      "description": "Form fields need labels or accessible names.",
      "fix": "Add label text and field identity."
    },
    {
      "id": "interaction.form-missing-submit-action",
      "principle": "Dependencies",
      "severity": "error",
      "automation": "automatic",
      "description": "Forms need a submit action or declared endpoint/handler.",
      "fix": "Add `[action=submit:...]` to the submit control or form."
    },
    {
      "id": "state.required-state-missing",
      "principle": "Dependencies",
      "severity": "warning",
      "automation": "heuristic",
      "description": "Interactive or dynamic components often need empty, loading, error, disabled, hover, or focus states.",
      "fix": "Add required states or declare why they are not needed."
    },
    {
      "id": "height.fixed-height-without-reason",
      "principle": "Exceptions",
      "severity": "warning",
      "automation": "automatic",
      "description": "Fixed height must be intentional.",
      "fix": "Use hug/min-height or add `[height=fixed] [reason=...]`."
    },
    {
      "id": "overflow.hidden-without-policy",
      "principle": "Exceptions",
      "severity": "warning",
      "automation": "automatic",
      "description": "Clipped surfaces need explicit overflow policy.",
      "fix": "Add `[overflow=hidden]`, `[overflow=visible]`, `[overflow=scroll]`, or `[overflow=truncate]`."
    },
    {
      "id": "overflow.text-clipping-risk",
      "principle": "Geometry",
      "severity": "error",
      "automation": "heuristic",
      "description": "Text appears likely to clip due to fixed height or insufficient bounds.",
      "fix": "Use hug height, min-height, or truncation policy."
    },
    {
      "id": "asset.raster-text-without-reason",
      "principle": "Exceptions",
      "severity": "error",
      "automation": "heuristic",
      "description": "Text-like content should not be exported as a raster asset without reason.",
      "fix": "Use native text or add asset exception reason."
    },
    {
      "id": "asset.image-missing-focal-point",
      "principle": "Geometry",
      "severity": "warning",
      "automation": "manual",
      "description": "Responsive images need a crop/focal strategy.",
      "fix": "Declare focal point, fit mode, or crop policy."
    },
    {
      "id": "asset.media-missing-fallback",
      "principle": "Exceptions",
      "severity": "warning",
      "automation": "manual",
      "description": "Video/Lottie/complex media should define fallback behavior.",
      "fix": "Add poster, fallback asset, controls, and autoplay policy."
    },
    {
      "id": "design-system.token-missing",
      "principle": "Geometry",
      "severity": "warning",
      "automation": "heuristic",
      "description": "Color, typography, radius, or spacing values appear detached from tokens.",
      "fix": "Use design tokens or declare an exception."
    },
    {
      "id": "accessibility.touch-target-too-small",
      "principle": "Geometry",
      "severity": "warning",
      "automation": "automatic",
      "description": "Interactive target is smaller than recommended touch size.",
      "fix": "Increase hit area or declare target-specific exception."
    },
    {
      "id": "accessibility.contrast-risk",
      "principle": "Exceptions",
      "severity": "warning",
      "automation": "heuristic",
      "description": "Text/background contrast may be insufficient.",
      "fix": "Adjust colors or document intentional exception."
    },
    {
      "id": "component.page-instance-declares-component-tag",
      "principle": "Dependencies",
      "severity": "warning",
      "automation": "heuristic",
      "description": "Page instances should not duplicate Figma component source with `[component=...]` tags.",
      "fix": "Remove `[component=...]` from page instance names and rely on Figma/UI Kit metadata."
    },
    {
      "id": "component.required-state-missing",
      "principle": "Dependencies",
      "severity": "warning",
      "automation": "heuristic",
      "description": "UI Kit component source is missing required states for its component family.",
      "fix": "Add states to the UI Kit component set instead of drawing them on a page."
    },
    {
      "id": "routing.page-route-missing",
      "principle": "Dependencies",
      "severity": "error",
      "automation": "automatic",
      "description": "Page roots must declare a concrete route or route pattern.",
      "fix": "Add `[route=/path]` for concrete pages or `[route-pattern=/path/:param]` for templates."
    },
    {
      "id": "routing.duplicate-route",
      "principle": "Dependencies",
      "severity": "error",
      "automation": "automatic",
      "description": "Two concrete pages must not declare the same route unless intentionally modeled as views of one page.",
      "fix": "Use one `[page=...]` with multiple `[view=...]` states, or change the duplicate route."
    },
    {
      "id": "routing.href-internal-route-missing",
      "principle": "Dependencies",
      "severity": "error",
      "automation": "automatic",
      "description": "Internal href points to a route that is not declared in the design contract.",
      "fix": "Add a page with the matching `[route=...]` or correct the href."
    },
    {
      "id": "routing.href-anchor-missing",
      "principle": "Dependencies",
      "severity": "error",
      "automation": "automatic",
      "description": "Internal href points to an anchor that is not declared on the target page.",
      "fix": "Add `[anchor=...]` to the target section or correct the href fragment."
    },
    {
      "id": "routing.page-view-route-drift",
      "principle": "Breakpoints",
      "severity": "error",
      "automation": "automatic",
      "description": "Views or breakpoints of one page identity must not use different routes.",
      "fix": "Keep one `[route=...]` across all `[view=...]` and `[bp=...]` variants of the page."
    },
    {
      "id": "variation.breakpoint-content-drift",
      "principle": "Breakpoints",
      "severity": "error",
      "automation": "heuristic",
      "description": "Content changed across breakpoints without a non-responsive variation axis.",
      "fix": "Keep breakpoint content identical, or model the change with `[view=...]`, `[locale=...]`, `[experiment=...]`, or another explicit axis."
    },
    {
      "id": "variation.theme-content-drift",
      "principle": "Exceptions",
      "severity": "error",
      "automation": "heuristic",
      "description": "Theme variants must not change text content.",
      "fix": "Keep content identical across themes; theme should change visual tokens only."
    },
    {
      "id": "variation.axis-overload",
      "principle": "Exceptions",
      "severity": "warning",
      "automation": "heuristic",
      "description": "One variation axis appears to hide a different reason for variation.",
      "fix": "Use the correct axis: breakpoint for layout, view for data state, locale for translation, experiment for A/B, role-view for permissions."
    },
    {
      "id": "adapter.unsupported-feature",
      "principle": "Exceptions",
      "severity": "warning",
      "automation": "automatic",
      "description": "The selected target adapter does not support a declared feature.",
      "fix": "Change the design, add asset fallback, or choose another adapter."
    }
  ]
}
